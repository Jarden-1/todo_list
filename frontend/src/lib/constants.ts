// Shared frontend constants. Keep magic values out of component code so they
// can be updated in one place.

// Fallback color for projects that don't have an explicit color set. Used in
// sidebar, todo cards, project views, completed view, and move dialog.
export const DEFAULT_PROJECT_COLOR = "#6366F1";
